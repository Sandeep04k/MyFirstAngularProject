import { MydirtDirective } from './mydirt.directive';

describe('MydirtDirective', () => {
  it('should create an instance', () => {
    const directive = new MydirtDirective();
    expect(directive).toBeTruthy();
  });
});
